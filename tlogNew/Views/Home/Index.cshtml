@{
    ViewBag.Title = "blogphins";
}
<style>
    .boxi {
        transition: box-shadow .3s;
        cursor: pointer;
    }
        .boxi:hover {
            box-shadow: 0 0 20px rgb(0, 0, 0);
        }
</style>

<div class="full text-center h-100  w-100 ">
    
    <div class="container" style="padding-top:13%">
          <p class="text-primary display-1 d-inline-block mt-3 text-lowercase">blog <kbd>phins</kbd></p>
    </div>
    <div>
        <h5 class="mt-3">Best Place to share anything you want.</h5>
        <button style="border-radius:50px" class="btn  btn-outline-dark mt-3"
         onclick="location.href='@Url.Action("Choose", "UserAccount", new { uid = Session["uid"],uname=Session["uname"] })'">
       
         Start Writing Now</button>
    </div>

</div>

<div class=" mob" style="margin-top :30%">
    <div class="d-inline-block mb-4 ml-2">
        <a style="font-size:3vh" class="text-lowercase pr-4" href="/">
            <img src="https://img.icons8.com/ios-filled/80/000000/dolphin.png">
        </a>

    </div>
    <div class="w-100 text-center" >
        <h2 class="display-4 d-inline-block mt-3 text-lowercase">blog <kbd>phins</kbd></h2>
        <div>
            <p class="mt-2">Best Place to share anything you want.</p>
            <button class="btn br btn-outline-dark btn-sm mt-1"
                    onclick="location.href='@Url.Action("UserProfile", "UserAccount", new { uid = Session["uid"],uname=Session["uname"] })'">

                Start Writing Now
            </button>
        </div>
    </div>

</div>